import { InjectionToken } from '@angular/core';

export const catogoryToken = new InjectionToken('catogoryToken');

export const categories = {
  cats: [
    'Drama',
    'Super-Hero',
    'Comedy',
    'Cartoon',
    'Action',
    'Science-Fiction',
  ],
};
